/**
 * El catálogo de emociones del diario proviene de la API (`GET /Emociones`),
 * el mismo que gestiona el panel en "Gestión de emociones".
 * Ver `useEmociones`, `EmocionesService` y `ModalEmociones`.
 */
